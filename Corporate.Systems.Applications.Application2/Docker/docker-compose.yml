version: '2'
services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 27018:27017
    networks:
     - default
    hostname: mongodb
    #volumes:
    #- ./db:/data/db:rw

  app2:
    image: console-app2-demo